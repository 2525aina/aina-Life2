rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{userId}/{allPaths=**} {
      allow read;
      // For debugging: Allow write if user is authenticated, without checking UID match.
      allow write: if request.auth != null;
    }
  }
}
